#ifndef CONTROLLER_H
#define CONTROLLER_H
#include "Aria.h"
#include "ArNetworking.h"
class Controller
{
    /*
private:
    FILE *file = NULL;
    ArClientBase * myClient;

    void enable(ArNetPacket* packet);
    FILE * getFile(ArNetPacket* packet);
    void checkObject(ArNetPacket* packet);

    ArFunctor1C enanleCB;
    ArFunctor1C getFileCB;
    ArFunctor1C checkObjectCB;
    */
public:
    Controller();
    /*
    ~Controller();
    void requestEnable();
    int requestCheckObject();
    FILE* requestFile();
    */
};

#endif // CONTROLLER_H
